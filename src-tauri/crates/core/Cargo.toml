[package]
name = "sleepguard-core"
version = "0.1.0"
edition = "2021"
description = "Shared config, schedule resolution and shutdown executor for SleepGuard"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "6.0.0"
chrono = "0.4.45"
